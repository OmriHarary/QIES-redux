#!/bin/bash

testname="Agent login/logout"
frontend="./bin/frontend"
vsl="./test/resources/valid-services-list.txt"
input="$( dirname "${BASH_SOURCE[0]}" )/input.txt"
output="$( dirname "${BASH_SOURCE[0]}" )/output.txt"
expectedout="$( dirname "${BASH_SOURCE[0]}" )/expected.txt"
log="$( dirname "${BASH_SOURCE[0]}" )/log.log"
expectedlog="$( dirname "${BASH_SOURCE[0]}" )/expected.log"