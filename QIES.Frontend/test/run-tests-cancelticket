#!/bin/bash

for c in $( ls ./QIES.Frontend/test/cases/cancelticket )
do
    . ./QIES.Frontend/test/cases/cancelticket/$c/vars
    . ./QIES.Frontend/test/run-test
    if [[ $? == "0" ]]
    then
        echo "Success"
    else
        echo "Fail"
    fi
done
