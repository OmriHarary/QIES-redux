#!/bin/bash

for c in $( ls ./QIES.Frontend/test/cases/login )
do
    . ./QIES.Frontend/test/cases/login/$c/vars
    . ./QIES.Frontend/test/run-test
    if [[ $? == "0" ]]
    then
        echo "Success"
    else
        echo "Fail"
    fi
done
